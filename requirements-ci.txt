# Pipeline-only dependencies for CI (GitHub Actions / SPCS).
# Includes matplotlib (needed for wind isoline extraction via ax.contour()).
# Does NOT include cartopy or scipy — those are only for notebooks/local dev; see requirements.txt.

# Core BUFR processing
eccodes>=1.5.0

# ECMWF data access
ecmwf-opendata>=0.3.29
requests>=2.28.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
xarray>=2023.1.0

# Geospatial processing
shapely>=2.0.0

# GRIB data processing
cfgrib>=0.9.10

# Wind isoline extraction — ax.contour() in ecmwf_wind_data_extractor.py
# Runs headless via MPLBACKEND=Agg (no display needed). cartopy/scipy not needed.
matplotlib>=3.7.0

# Snowflake
snowflake-connector-python[pandas]>=3.0.0
cryptography>=41.0.0

# Precipitation Zarr storage (requires Python >=3.11)
zarr>=3.0.0
numcodecs>=0.14.0
blosc2>=2.0.0
